#include <stdio.h>

/**
 * main: This program prints alphabet in lowercase 
 * followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c <= z)
	{
		putchar("%c\n", c++);
	}
	return (0)
}
