#include "main.h"

/**
 * _islower - This function checks for lowercase character.
 *
 * Return: Always 0.
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return 1;
	}
	else
	{
		return 0;
	}
}

int _islower(int c)
{
	char ch = 'a';
	if (_islower(ch))
	{
		_putchar(ch);
	}
	else 
	{
		_putchar(ch);
	}
	_putchar('\n');
}	
